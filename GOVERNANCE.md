# Governance

`wilsonle/speech` is maintained as an open-source local-first speech recognition project.

Decisions that affect architecture, privacy guarantees, model/profile contracts, benchmark methodology, deployment headers, or licensing gates should be recorded with an ADR-style instruction document under `docs/instructions/`.

The project prioritizes deterministic behavior, local privacy, clear licensing, and measurable latency over speculative features.
