# Third-Party Notices

This file tracks notices for runtime, build, and model-development dependencies that require attribution beyond package metadata.

Initial foundation dependencies are managed through `package.json`, `pnpm-lock.yaml`, and `pyproject.toml`. Model-pack notices must be added before publishing downloadable model artifacts.
