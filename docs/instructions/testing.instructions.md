---
description: 'Validation commands, CI expectations, and test categories for speech.'
applyTo: '**/*.{ts,tsx,js,mjs,py,json,yml,yaml},apps/**,packages/**,tools/**,training/**'
---

# Testing and validation

Run the smallest useful validation set for each change, but keep these repository-level commands healthy:

```bash
pnpm lint
pnpm format-check
pnpm typecheck
pnpm test
pnpm build
pnpm chromium-smoke
```

Required CI checks are lint, format-check, typecheck, unit tests, Python tests, production build, Chromium smoke, and license/generated-file checks.

Performance gates must be measured from audio timestamps and documented with browser, OS, hardware, model version, provider, thread count, and power state. Shared CI performance is informational unless it runs on designated reference hardware.

- v0.6 task-first UI changes should treat `docs/planning/v0.6.0-ui-inventory.json` plus ADR 0008 as the v0.5 parity baseline for routes, actions, workflow states, and user-facing strings. Keep `training/tests/test_v0_6_ui_inventory.py` green when the baseline artifact or its allowed destination vocabulary changes; later UI-route tests should prove migrated v0.6 paths rather than weakening the baseline snapshot.
- v0.6 primitive and feature-screen accessibility tests should reuse `@speech/ui/testing` primitive examples and `auditSpeechPrimitiveExampleMarkup`/`auditSpeechPrimitiveInteractionCoverage` instead of duplicating local assumptions. Keep `packages/ui/src/testing/primitiveAccessibility.test.tsx` green when adding primitives or changing keyboard, Escape, focus-order/restoration, ARIA state, hidden-focusability, pointer/touch, forced-colours, reduced-motion, disabled/loading, or tokenized CSS-state requirements.
- v0.6 baseline task metrics live in `docs/planning/v0.6.0-baseline-task-metrics.json` with ADR 0009. Keep `training/tests/test_v0_6_baseline_task_metrics.py` green if the local screenshot manifest, task counts, privacy flags, or baseline audit wording changes; this artifact is a local audit baseline, not moderated usability evidence. Task-first IA decisions live in ADR 0010; keep `training/tests/test_v0_6_task_first_information_architecture.py` green if route ownership, primary destinations, app-menu destinations, legacy redirect intent, or domain-contract boundary wording changes. Progressive-disclosure decisions live in ADR 0011; keep `training/tests/test_v0_6_progressive_disclosure_rules.py` green if visible-vs-disclosed content rules, mechanism prohibitions, minimal-copy budgets, accessibility/privacy visibility, or worker-boundary wording changes. Terminology and copy-budget decisions live in ADR 0012 plus `docs/planning/v0.6.0-terminology-copy-budgets.json`; keep `training/tests/test_v0_6_terminology_copy_budgets.py` green if mapped default UI terms, copy-budget surfaces, allowed exception reasons/fields, or technical-term constraints change. Route-map and legacy-redirect decisions live in ADR 0013 plus `docs/planning/v0.6.0-route-map.json`; keep `training/tests/test_v0_6_route_map.py` green if target paths, v0.5 hash-anchor redirects, safe query keys, safe return-target rules, focus/scroll expectations, or route-boundary wording changes. Semantic visual tokens live in `@speech/ui` plus `docs/planning/v0.6.0-token-bundle-impact.json`; keep `packages/ui/src/tokens/tokens.test.ts` and `training/tests/test_v0_6_semantic_tokens.py` green if token categories, CSS entrypoints, app imports, bundle-impact thresholds, reduced-motion, forced-colours, focus, touch-target, or responsive token hooks change. Button/IconButton primitives also live in `@speech/ui`; keep `packages/ui/src/primitives/button.test.tsx` green if native-button semantics, default `type=button`, disabled/loading/destructive states, icon-only accessible names, optional tooltip descriptions, keyboard activation fixtures, or tokenized focus/touch/forced-colours/reduced-motion CSS changes. Disclosure/Accordion primitives live in `@speech/ui`; keep `packages/ui/src/primitives/disclosure.test.tsx` green if native `<details>/<summary>` disclosure semantics, accordion `aria-expanded`/`aria-controls`, hidden collapsed panels, heading labels, keyboard focus fixtures, no-nested-accordion usage rules, or tokenized focus/touch/forced-colours/reduced-motion CSS changes. MenuButton/Tooltip primitives live in `@speech/ui`; keep `packages/ui/src/primitives/menu.test.tsx` green if menu trigger/menuitem semantics, action/navigation-only menus, no submenu/form usage rules, keyboard focus helpers, Escape/focus-return behavior, tooltip `aria-describedby`, plain-text tooltip content, hidden states, or tokenized focus/touch/popover/forced-colours/reduced-motion CSS changes. Dialog/Select/RadioGroup primitives live in `@speech/ui`; keep `packages/ui/src/primitives/dialog.test.tsx` and `packages/ui/src/primitives/form-controls.test.tsx` green if modal labelling/descriptions, focus-trap/restore/Escape semantics, native labelled select/radio controls, invalid/required descriptions, sanitized radio DOM IDs, keyboard fixtures, or tokenized backdrop/form/focus/forced-colours CSS changes. Feedback/progress primitives live in `@speech/ui/feedback`; keep `packages/ui/src/feedback/feedback.test.tsx` green if InlineError, Notice, Toast, EmptyState, LoadingState, Progress, Status, live-region defaults, native progress text equivalents, non-toast-only blocker rules, hidden states, or tokenized feedback/progress/reduced-motion/forced-colours CSS changes.
- Keep `apps/web/src/state/uiPreferences.test.ts` green when changing `UiPreferencesV1`, the dismissed-hint registry, legacy UI-only migration keys, default values, namespaced storage key, reset semantics, or rollback/corrupt-storage behavior; tests must prove UI preference deletion leaves domain stores untouched.
- Keep `apps/web/src/app/routeState.test.ts`, `App.test.tsx`, `SettingsPanel.test.tsx`, `appShellStatus.test.ts`, and shell-focused Playwright coverage green when changing `AppShell`, primary destination routes, app-menu items, Settings index/Audio/Storage routes, Local status summaries/popovers, heading-focus behavior, skip links, desktop navigation, narrow bottom navigation, safe-area padding, or no-horizontal-overflow guarantees. Route-state tests must cover v0.5 hash redirects, v0.6 path routes, safe query and return-target sanitization, focus/scroll restoration plans, and navigation-guard confirmation decisions for disruptive recording/enrollment/training/import/export/model-update work. App-menu/Local-status tests must prove keyboard/Escape behavior and aggregate-only output without hashes, provider names, storage totals, raw model names, or private IDs. Settings/Audio tests must prove `/settings` lists Audio/Appearance/Storage/Privacy/Shortcuts/Diagnostics/About without creating cosmetic controls, and `/settings/audio` exposes microphone processing, recording mode, explicit input test, reset calibration, and disclosed actual track/sample-rate/level/worklet diagnostics using a fake microphone without persisting audio. Storage route tests must cover aggregate base-model/profile/recording-training/quota summaries, dedicated confirmation screens for training data, one voice model, one speech-model download, and all local speech data, plus removed/retained consequences with no raw IDs/hashes/paths. Dictate workspace changes must keep focused transcript unit/E2E coverage green for setup-only fresh installs with mocked tiny model downloads, seeded-installed-model capture flows, one-action recording, Space shortcut, hidden empty transcript actions, transcript action menu download/clear, details-disclosed latency metrics, concise microphone-blocker recovery copy, copy/download privacy, and release-validation stress/no-network behavior. Enrollment prompt UI changes must keep helper/unit and fake-microphone E2E coverage for one-prompt progress, Whisper/Normal/Loud labels, safe Loud guidance, useful-only condition hints, single record/stop control, explicit quality reason-to-copy mappings, low-recognizer-confidence accept/review wording, restrained prompt/result live regions, replay/retry/accept/skip/pause/reload recovery, details-disclosed metrics/storage, and no raw reason-code/utterance/checksum leakage in default feedback. Vocabulary list/editor/reason-code changes must keep focused browser coverage for set search, visible New set action, row-open behavior, on/off state, term counts, overflow actions, explicit import/export flow, basic Add/Update word fields, Advanced-disclosed steering/category/case/prompt settings, prompt-preview/diagnostics visibility, CRUD/import/export round trips, and the conditional “Applies next recording” boundary message. Keep `apps/web/src/content/reasonCodes.test.ts`, vocabulary-storage tests, and Personal Models helper/UI tests green when adding or changing stable vocabulary/model reason-code mappings; tests should prove default copy avoids raw schema/parser messages, WER/CER/gate/adapter/OPFS/WebGPU/WASM/base-model jargon, raw IDs, hashes, storage paths, and private vocabulary terms. Offline/update UI tests must assert aggregate ready/blocker/update summaries and open lifecycle details explicitly before checking catalog/update internals.
- Numerical parity fixtures (log-Mel, transcript, tiny-adapter) live under `test-data/expected/*.json` and are regenerated by `tools/*/generate_*_reference.py`; TS tests load the fixture and compare against the in-package implementation, while Python tests reproduce the fixture from the reference module. The tiny-adapter fixture (`test-data/expected/tiny-adapter-parity.json`) pins a deterministic 50-epoch frozen-feature affine-adapter trajectory (initial/final loss, per-checkpoint weights/bias/checksum, and runtime-adapter forward output) shared by `@speech/browser-training` backend math and `tools/training-parity`; when changing adapter math, regenerate with `PYTHONPATH=tools/training-parity uv run python tools/training-parity/generate_tiny_adapter_reference.py` and confirm both the TS and Python parity tests still pass within tolerance. If browser-training defaults move to AdamW/scheduled optimization, keep the parity fixture on its explicit `sgd` trajectory and add separate focused tests for sampler, AdamW, validation, early-stopping, gradient-clipping, finite-value guards, resume-state checksum validation, pause/resume output matching uninterrupted training, Web Locks/BroadcastChannel cross-tab coordination redaction/busy-state behavior, ASR-priority cooperative pause, and session resource cleanup/disposal.
- Personal Models navigation/list/preflight/create/detail/readiness/import UI changes need focused helper tests for aggregate-only row/card/preflight/detail/readiness/import data, Playwright coverage for empty/generic-fallback, compact model rows, state-dependent primary actions, row-menu rename/duplicate/export/deactivate/rollback/delete actions, capability, training-companion, missing-recording-task, saved-profile, visible hard blockers, `/models/new` one-question create-model wizard steps, `/models/:profileId/train` blocker-first readiness with recording counts/duration/additional-free-storage/browser-support/support-file status, `/models/import` choose/unlock/validate/review route flow with legacy JSON import disclosed under details and hostile `.speechmodel` rejection, `/models/:profileId/export` contents/encryption/review/save route flow with legacy profile export disclosed under details and non-exportable profile rejection, UI-only local draft persistence, details-disclosed architecture copy, keyboard/screen-reader focus, Start recording handoff to enrollment, and model-detail accordion sections for recording coverage, quality results, compatibility, storage, and technical details. Run a browser visual review across desktop/mobile when layout changes. Multi-profile lifecycle changes also need package tests for list/rename/dedupe/replace/import-as-new/name-collision/delete-active-pointer/deactivate behavior and worker/client typecheck coverage. Avoid duplicating exact public model display names across unrelated sections when existing strict locators depend on unique text.
- Browser-training progress/recovery UI changes need focused helper tests for the user-facing Preparing/Training/Checking/Ready stage mapping, redacted recovery/resource summaries, safe-boundary pause/cancel/restart/resume controls, live-region progress text, text equivalents for stage/progress visuals, destructive-action confirmations, disclosed Training details for epoch/loss/LR/backend/threads/memory/checkpoint data, reload recovery after page refresh, cross-tab lock contention, ASR-priority pause, reduced-motion/narrow-viewport accessibility checks, and desktop/mobile visual review when layout changes. Cross-browser/fault-injection hardening should keep Chromium green by default and make Edge opt-in (`SPEECH_E2E_EDGE=1`) on hosts with Microsoft Edge installed; cover worker faults, quota/recovery failure, cross-tab contention, hostile imports, export/import, activation, rollback, and deletion without adding private fixtures.
- Personal/anchor end-to-end evaluation changes need focused package tests for `generic`/`p1`/`candidate` aggregate metrics, personal-heldout improvement, anchor and slice regression gates, RTF overhead relative to P1, adapter-size limits, false-insertion budgets, and privacy serialization checks that reject raw case IDs, text, selected-vocabulary IDs, audio, feature tensors, checkpoints, or adapter weights. Bilingual cohort gate changes need tests for missing-data release blocking, 30-speaker pass/fail math, language/accent/voice-condition slice regression, duplicate speaker input rejection, and privacy serialization that omits raw speaker/profile identifiers. Reference benchmark gate changes need package tests for missing/synthetic-insufficient evidence, complete passing reference-hardware measurements, hard budget failures, privacy serialization, and diagnostics export inclusion. Dictate performance parity changes need package tests for missing reference evidence, passing/failing reference-hardware gates, CI browser-smoke insufficient-evidence handling, privacy redaction, diagnostics export inclusion, and release-validation browser smoke for JS/CSS bytes, interaction readiness, route transition, long tasks, CLS, recording UI response, and ASR-latency limitations. Activation-decision and candidate-result UI changes also need helper tests for ready/review-needed/blocked/not-ready outcomes, hard-vs-advisory check groups, disabled activation on hard-check failure, metric grouping under Personal speech/Languages/Voice levels/Vocabulary/General speech/Performance, rollback/generic-fallback assertions, privacy redaction, focused route/focus E2E for `/models/:profileId/results`, and desktop/mobile visual review when rendered UI changes.
- Portable `.speechmodel` encryption/importer changes need focused tests for unencrypted explicit-export round trip, encrypted AES-GCM/PBKDF2 round trip, deterministic salt/IV fixture behavior, no plaintext leakage in envelope bytes, minimum iteration enforcement, missing/wrong passphrase handling, tampered ciphertext rejection, abort checks, controlled Web Crypto/base64 error paths, envelope-to-inner-bundle importer round trips, wrong-passphrase importer rejection, hostile payload rejection for raw privacy flags, base-model/operator/external-data media/types, unsafe paths, duplicate/colliding files, missing refs, checksum mismatch, trailing bytes, malformed JSON, archive-bomb file counts/arrays, forbidden tensor payload keys, parser fuzz mutations for invalid magic/version/GCM tag, plus exact-base compatibility, worker-owned smoke-vector execution, runtime-smoke timeout cleanup, temporary staging cleanup on failure, committed-record-last atomic import behavior, and CLI residual-adapter `.speechmodel` coverage that preserves the original residual-adapter profile manifest, requires exact base/tokenizer identity, and proves unencrypted CLI output is explicit opt-in. Speech profile V1-to-V2 migration changes need a committed synthetic v0.4.0 fixture plus package tests for V1 dispatch loading, copy-on-write `profile.v2.json`, stale temp cleanup, valid-target resume, invalid-target replacement from retained V1, missing-source failure, V2 residual-adapter runtime compatibility, and aggregate-only migration summaries. Portable export UI changes need package tests for encrypted `.speechmodel` generation from exportable adapter artifacts, exact-base mismatch rejection, non-exportable enrollment-profile rejection, worker/client transfer of encrypted bytes, route helper unit tests for passphrase/review/error copy, and browser smoke coverage for the dedicated export route and legacy Details disclosure.

- Component gallery changes must keep `apps/web/src/app/component-gallery.test.tsx` and `training/tests/test_v0_6_component_gallery.py` green. The gallery contract in `docs/planning/v0.6.0-component-gallery-contract.json` must continue to document the dev-only `#ui-gallery` route, complete current primitive coverage, synthetic fixture privacy rules, and production bundle impact with no gallery chunk emitted.
