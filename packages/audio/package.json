{
  "name": "@speech/audio",
  "version": "0.5.0",
  "private": true,
  "description": "Audio transport primitives: capture controller, ring buffer, fallback buffers, and resampling integration.",
  "type": "module",
  "license": "Apache-2.0",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests --exclude=dist/**"
  },
  "devDependencies": {},
  "dependencies": {
    "@speech/protocol": "workspace:*"
  }
}
