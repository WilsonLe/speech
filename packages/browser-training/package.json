{
  "name": "@speech/browser-training",
  "version": "0.1.0",
  "private": true,
  "description": "BrowserTrainingBackend contracts and fixed local adapter-math implementation for in-browser personal models.",
  "type": "module",
  "license": "Apache-2.0",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests --exclude=dist/**"
  },
  "dependencies": {
    "@speech/protocol": "workspace:*"
  },
  "devDependencies": {}
}
