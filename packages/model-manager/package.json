{
  "name": "@speech/model-manager",
  "version": "0.0.0",
  "private": true,
  "description": "Model catalog, install, checksum, storage, activation, rollback, and deletion lifecycle.",
  "type": "module",
  "license": "Apache-2.0",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@speech/protocol": "workspace:*"
  },
  "devDependencies": {}
}
