{
  "name": "@speech/personalization",
  "version": "0.6.0",
  "private": true,
  "description": "Speaker profile, personal/anchor evaluation, bilingual cohort release gates, activation decisions, adapter runtime, browser-vs-Python comparison, and residual/LHUC personalization contracts.",
  "type": "module",
  "license": "Apache-2.0",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests --exclude=dist/**"
  },
  "dependencies": {
    "@speech/browser-training": "workspace:*",
    "@speech/enrollment": "workspace:*",
    "@speech/protocol": "workspace:*"
  },
  "devDependencies": {}
}
