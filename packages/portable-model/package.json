{
  "name": "@speech/portable-model",
  "version": "0.6.1",
  "private": true,
  "description": "Portable per-user .speechmodel manifest, deterministic inner bundle, encrypted binary envelope, CLI residual-adapter compatibility, constrained importer, and hostile-import boundary contracts.",
  "type": "module",
  "license": "Apache-2.0",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests --exclude=dist/**"
  },
  "dependencies": {
    "@speech/protocol": "workspace:*"
  },
  "devDependencies": {}
}
