{
  "name": "@speech/portable-model",
  "version": "0.1.0",
  "private": true,
  "description": "Portable per-user .speechmodel manifest, deterministic inner bundle, binary envelope, and hostile-import boundary contracts.",
  "type": "module",
  "license": "Apache-2.0",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests --exclude=dist/**"
  },
  "dependencies": {
    "@speech/protocol": "workspace:*"
  },
  "devDependencies": {}
}
