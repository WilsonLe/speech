{
  "name": "@speech/protocol",
  "version": "0.1.0",
  "private": true,
  "description": "Versioned TypeScript contracts shared across UI, workers, tools, and tests.",
  "type": "module",
  "license": "Apache-2.0",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests --exclude=dist/**"
  },
  "devDependencies": {}
}
